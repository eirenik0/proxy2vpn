Agent investigations now compare sibling containers that share the same profile so healthy peers reduce suspicion of profile-wide or account-wide provider issues and isolated auth failures prefer tunnel restart before container recreation when the control API is reachable.
