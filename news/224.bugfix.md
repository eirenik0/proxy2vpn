The watchdog now treats isolated auth failures on an otherwise healthy shared profile as service-specific and restarts the tunnel before opening an auth incident, while investigations describe the same profile-wide vs service-specific distinction more explicitly.
