Rotation now retries alternate cities, renames the compose service/container to match the new city, records requested and final rotation changes for later agent investigation, and only succeeds when the replacement endpoint becomes healthy with a verifiably new egress IP.
